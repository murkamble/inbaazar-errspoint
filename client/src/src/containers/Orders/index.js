import React from 'react';
import Layout from '../../components/Layout';

const Orders = () => {
    return (
        <Layout sidebar>
            <p>Orders</p>
        </Layout>
    );
}

export default Orders;